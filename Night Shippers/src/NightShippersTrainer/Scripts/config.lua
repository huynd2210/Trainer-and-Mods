-- Night Shippers Trainer - settings.
-- Edit the values, save, restart the game.

return {
    -- How much gold one press gives.
    GoldAmount = 1000,

    -- Key that grants the gold. Valid names are Microsoft virtual-key names, e.g.
    -- "F10", "F5", "ADD" (numpad +), "INS", "HOME", "G".
    GiveGoldKey = "F10",

    -- Modifier keys that must be held, e.g. { "CONTROL" } or { "CONTROL", "SHIFT" }.
    -- Leave empty for no modifier.
    ModifierKeys = {},
}
